import { CustomValidators } from './custom-validators.validator';

describe('ContainSpaces.Validator', () => {
  it('should create an instance', () => {
    expect(new CustomValidators()).toBeTruthy();
  });
});
